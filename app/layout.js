export const metadata = {
  title: "SCL Tokenizer",
  description: "Convert text into multiple formats + token counts",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
